[project]
name = "ninjarmy"
version = "0.1.0"
dependencies = [
    "click",
    "pydantic",
    "pyyaml",
    "anthropic",
    "python-dotenv",
    "rich",
    "textual",
    "fastapi",
    "uvicorn[standard]",
    "websockets",
]

[project.scripts]
ninjarmy = "ninjarmy.cli.main:cli"